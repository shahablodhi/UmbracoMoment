@inherits Umbraco.Web.Mvc.UmbracoTemplatePage
@{
    var page = (dynamic)ViewBag.Page;
}
<link href="~/theme/moment-theme/Template/css/shortcodes.css" rel="stylesheet"/>
<script type="text/javascript" src="@Url.Content("~/theme/moment-theme/Template/js/plugins-jquery.js")"></script>
<script src="/scripts/instafeed.js"></script>
<!--=================================
owl carousel -->

<section id="@page.Id" style="position: relative; overflow: initial;" class="page-section-ptb">
    <div class="container">
        @if (page.showStripes)
        {
            <div class="overlay-stripes">
                <img class="img-responsive" src="~/theme/moment-theme/Template/images/lines_full.png" />
            </div>
        }
        <div class="row">
            <div class="col-lg-12 col-md-12">
                <div class="owl-carousel-15" id="instafeed">
                    @*@foreach (var slide in page.carousel)
                    {
                        <div class="item">
                            <img src="@Umbraco.Media(slide.image.ToString()).umbracoFile" alt="">
                        </div>
                    }*@

                </div>
            </div>
        </div>
    </div>
    <script>
        var feed = new Instafeed({
            get: 'user', userId: 307233874, clientId: '8956717d9d814ee3acdaa61c478fe832', accessToken: '307233874.1677ed0.eab47b786f62436998b4d32d6c99b54e', template: '<div class="item"><img src="{{image}}" alt=""></div>', resolution: 'low_resolution', limit: 10,
            after: function () {
                debugger;
                $('.owl-carousel-15').owlCarousel({
                    loop: true,
                    margin: 10,
                    autoplay: true,
                    autoplayTimeout: 2000,
                    autoplayHoverPause: true,
                    nav: true,
                    responsive: {
                        0: {
                            items: 1
                        },
                        600: {
                            items: 3
                        },
                        1000: {
                            items: 5
                        }
                    }
                });
            }
        });
        debugger;
        feed.run();
    </script>
</section>

<!--=================================
owl carousel -->
